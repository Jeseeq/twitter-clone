# twitter-clone
Kinda twitter clone


To run this example:

* clone repository  
```git clone https://github.com/Jeseeq/twitter-clone```
* run server in production mode
```export NODE_ENV=production```
```npm run start```
* run server in dev mode with hot reload
```npm run dev```

